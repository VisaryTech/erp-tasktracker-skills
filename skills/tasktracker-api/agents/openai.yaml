interface:
  display_name: "TaskTracker API"
  short_description: "Use TaskTracker strictly through generated Swagger artifacts and exclude Hidden=true in OData reads unless --include-hidden is explicit"
